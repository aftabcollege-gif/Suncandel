# Admin Guide

Seed admin (after `npx drizzle-kit push` and `npm run seed`):

- Username: `admin`
- Phone: `09155088324`
- Password: `admin@12345`

Login at `/admin/login` or `/login`.

- Monitor audit logs: `/api/v1/system/audit-logs`
- Monitor AI analytics: `/api/v1/ai/analytics/insights`
- Validate model registry: `/api/v1/ai/models`
