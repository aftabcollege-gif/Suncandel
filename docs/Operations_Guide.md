# Operations Guide

- Health checks: `/health`, `/api/health`
- Logs: structured JSON via `appLog`
- AI monitoring: `ai_inferences`, `ai_evaluation_runs`
- Incident flow: detect → isolate → rollback/fix → verify
